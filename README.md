## TO Excel
 This is a program desgined to create a spreadsheet supporting file from the csv's.
### Commands
 * efmt -h
   Prints the all commnds and usage with descriptions
 * efmt -c [input] [output]
    Reads From the input file and Generates the excel file into the provided output filename
  *efmt -d
    This flags is a delimeter flag you can pass the delimieter that seperates the data in the file
#### Usage
 efmt -d , -c file.csv file.xlxs
